#!/bin/bash

echo "Running deno fmt --check before push..."
/home/gg/.deno/bin/deno fmt --check
if [ $? -ne 0 ]; then
    echo -e "${RED}Error: deno fmt --check failed. Please fix formatting issues before pushing.${NC}"
    exit 2
fi
echo -e "${GREEN}Formatting check passed.${NC}"
exit 0
